Python 스크립트를 실행파일로 배포하기.

1) PyInstaller 패키지 설치.
pip install pyinstaller

2) Python 파일이 있는 폴더로 이동한 다음, 대상 파일을 지정하면 폴더에 dist 경로 생성됨.
ex) pyinstaller GitCloneTest.py

3) 콘솔창이 보이지 않게 하려면...
   pyinstaller -w GitCloneTest.py

4) 실행 파일 하나만 생성되게 하려면..
    pyinstaller -w -F GitCloneTest.py